{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the one window needs",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "midi:default",
    "sql:default",
    "sql:allow-load",
    "sql:allow-select",
    "sql:allow-execute"
  ]
}
