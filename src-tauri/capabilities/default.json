{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions the one window needs",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "midi:allow-open-input",
    "midi:allow-close-input",
    "sql:default",
    "sql:allow-execute"
  ]
}
