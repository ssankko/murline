// The Audio dialog's Effects section: the ordered chain of effects after the instrument.

export function EffectsSection() {
  return (
    <section className="flex flex-col gap-2">
      <h3 className="text-[13px] font-semibold">Effects</h3>
    </section>
  );
}
